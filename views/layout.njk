<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Wine Reviews</title>
  <link rel="stylesheet" href="/style.css">
</head>
<body>

  <div class="navbar">
    <div class="inner">

      <div class="brand">
        <div class="brand-badge"></div>
        <a href="/" class="brand">Wine Reviews</a>
      </div>

      <div class="nav-links">
        <a class="pill" href="/">Home</a>

        {% if user %}
          <span class="pill success">Hello {{ user.username }}</span>
          <a class="pill" href="/me/reviews">My reviews</a>

          {% if user.role == "admin" %}
            <a class="pill primary" href="/admin/wines">Admin</a>
          {% endif %}

          <a class="pill danger" href="/logout">Logout</a>
        {% else %}
          <a class="pill" href="/login">Login</a>
          <a class="pill primary" href="/register">Register</a>
        {% endif %}
      </div>

    </div>
  </div>

  <div class="container">
    {% block content %}{% endblock %}
  </div>

</body>
</html>
